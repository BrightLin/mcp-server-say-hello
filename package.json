{
  "name": "mcp-server-say-hello",
  "version": "0.0.1",
  "description": "The most simple ModelContext Protocol server example, just say hello to user",
  "license": "MIT",
  "author": "Bright.lin",
  "type": "module",
  "bin": {
    "mcp-server-say-hello": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && shx chmod +x dist/*.js",
    "prepare": "npm run build",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.8.0",
    "util": "^0.12.5",
    "zod-to-json-schema": "^3.24.5"
  },
  "devDependencies": {
    "@types/node": "^22.15.2",
    "shx": "^0.3.4",
    "typescript": "^5.6.2"
  }
}
